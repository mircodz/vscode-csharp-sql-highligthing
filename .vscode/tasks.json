{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "bun install && bun run build",
      "group": "build",
      "problemMatcher": ["$tsc"]
    }
  ]
}
